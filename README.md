# Point Mall 실습 Django API Server
Point Mall API Server
# 환경설정
python -m venv --copies venv<br>
source venv/bin/activate<br>
pip install -r requirements.txt
# 서버 실행
source venv/bin/activate<br>
python manage.py runserver
